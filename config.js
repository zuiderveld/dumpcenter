// Dump Center — frontend config (Vercel + lokaal)
window.DUMP_CENTER_CONFIG = {
    // Backend URL. Leeg = zelfde origin (lokaal start.bat).
    // Op Vercel: vul in met je publieke backend (tunnel/VPS), bijv. "https://api.jouwdomein.nl"
    apiBase: "",

    // Waar gebruikers na login terugkomen (Vercel site)
    frontendUrl: "https://dumpcenter-m79v.vercel.app",

    // Discord app (publiek — mag in frontend)
    discordClientId: "1133760156052754502",
};
